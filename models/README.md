# Models

Place local model weights here.

Expected fight model path:

```text
models/fight/best.pt
```

Model weights are intentionally ignored by Git because they can become large quickly. If a model must be versioned, use Git LFS or document a download source.
